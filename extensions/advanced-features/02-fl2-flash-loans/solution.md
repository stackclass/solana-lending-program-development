In this stage, you'll implement flash loans.

### Step 1: Implement solution

Add flash_loan function that allows borrowing without collateral within a transaction.

### Step 2: Run tests

```sh
git add .
git commit --allow-empty -m "[any message]"
git push origin main
```
