In this stage, you'll implement a dynamic interest rate model.

### Step 1: Implement solution

Add interest rate parameters to Bank and calculate rates based on utilization.

### Step 2: Run tests

```sh
git add .
git commit --allow-empty -m "[any message]"
git push origin main
```
